# Google OAuth — Session Log: 2 Haziran 2026

## Eski Client (Desktop uygulamasi - CALISMADI)
- Client ID: `GOOGLE_CLIENT_ID_REDACTED`
- Sorun: `urn:ietf:wg:oauth:2.0:oob` redirect_uri Cloud Console'da tanimli degildi
- Hata: `redirect_uri_mismatch` (400)

## Yeni Client (Web uygulamasi - KISMEN CALISTI)
- Client ID: `GOOGLE_CLIENT_ID_REDACTED`
- Client Secret: `GOCSPX-REDACTED_CLIENT_SECRET`
- Application type: **Web application**
- **Authorized redirect URIs:**
  - `http://localhost:8763` (eklendi)
  - `urn:ietf:wg:oauth:2.0:oob` (EKLE - GEREKLI OLABILIR)
  - `https://n8n.aiergene.xyz/webhook/google-oauth` (EKLE - public webhook)
- **Sonuc**: OOB calismadi (`400 invalid_request`), IP calismadi (`tld required`)

## Nasil Cozuldu

Google 2026'da yeni OAuth client'lar icin:
- `urn:ietf:wg:oauth:2.0:oob` -> REDDEDILDI (404/400)
- `http://193.164.4.149:8763` -> REDDEDILDI (TLD gerekli)
- `http://localhost:8763` -> SADECE ayni makinedeki tarayici icin gecerli

**Tek cozum**: Public domain'de bir webhook endpoint'i kullanmak.

### n8n Webhook Cozumu

1. n8n'de manuel olarak webhook workflow'u olustur
2. Veya SQLite uzerinden direkt ekle:
   - `workflow_entity` -> nodes (JSON), connections (JSON), settings
   - `webhook_entity` -> webhookPath, method
   - `shared_workflow` -> role='workflow:owner', projectId
   - `workflow_history` -> versionId, nodes, connections
   - `workflow_published_version` -> publishedVersionId

3. n8n container'ini restart et
4. Webhook test et: `curl https://domain.com/webhook/path?code=test`

### Kod Node'u Detaylari

```javascript
const items = $input.all();
const item = items[0] || {json: {}};
const query = item.json.query || {};
const code = query.code || '';
return [{json: {success: true, code: code}}];
```

**Puf noktalar:**
- `$input` shell'de bash degiskeni olarak yorumlanir -> bir Python dosyasindan okuyarak gonder
- `fs` modulu yasak -> dosyaya yazmak yerine return et
- `$input.first()` n8n v2'de calismaz -> `$input.all()[0]` kullan
- `$json` webhook GET sorgusunda calismaz -> `$input.all()[0].json` kullan

## Ogrenilenler

1. **Application type onemli**: "Desktop application" ile varsayilan redirect URI'leri yetersiz.
   "Web application" ile explicit redirect URI eklemek lazim.
2. **OOB ve IP reddediliyor**: Public domain webhook tek guvenilir cozum.
3. **n8n SQLite injection**: API calismadiginda direkt veritabanina yazmak ise yariyor.
4. **Port 8080 DOLU** -> 8763 guvenli alternatif.
5. **n8n container restart gerekli**: Her workflow degisikligi sonrasi.

## Dosyalar
- `~/.google/credentials.json` — yeni client bilgileriyle guncellendi
- `~/hermes_data/google_api.py` — 5 scope'lu API fonksiyonlari
- `~/hermes_data/google_auth_server.py` — port 8763'te local web server ile auth
- `~/hermes_data/google_auth_server.py` -> `~/hermes_data/gonder/` (kullanilmadi)
- n8n SQLite: `/var/lib/docker/volumes/y10hlm1fr9avvxt3asz5p0bk_n8n-data/_data/database.sqlite`
